#!/bin/sh

if [ "$1" = "configure" ]; then
        if [ -d /usr/doc -a -h /usr/doc/bible-kjv-text -a -d /usr/share/doc/bible-kjv-text ]; then
                rm -f /usr/doc/bible-kjv-text
        fi
fi

exit 0
